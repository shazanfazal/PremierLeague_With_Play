# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
# GET     /premierLeagueTeam        controllers.HomeController.getListOfPremierLeague
# POST    /premierLeagueTeam        controllers.HomeController.getListOfPremierLeague


# Map static resources from the /public folder to the /assets URL path
# GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

# dilums routes
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Serve index page from public directory
GET     /                     controllers.FrontendController.index()
GET     /getTeams          controllers.PremierLeagueController.getTeams
GET     /getMatchesPlayed   controllers.PremierLeagueController.getMatches
GET     /sortTeamsWins      controllers.PremierLeagueController.sortByWins

# Serve static assets under public directory
GET     /*file                      controllers.FrontendController.assetOrDefault(file)
